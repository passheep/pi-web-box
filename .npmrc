fund=false
# npm run 会把这里的配置注入成 npm_config_electron_mirror 环境变量，
# 其优先级高于 electron-builder.yml 的 electronDownload.mirror。
# 写在这里可以覆盖用户级 npmrc 中可能残留的失效镜像（例如已停止服务的 npm.taobao.org）。
electron-mirror=https://npmmirror.com/mirrors/electron/